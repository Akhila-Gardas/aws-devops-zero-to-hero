version: 0.2

env:
 parameter-store:
 DOCKER_REGISTRY_USERNAME: /myapp/docker-credentials/username
 DOCKER_REGISTRY_PASSWORD: /myapp/docker-credentials/password

phases:
 install:
 runtime-versions:
 python: 3.11
 pre_build:
 commands:
 - echo "Logging in to DockerHub..."
 - echo "$DOCKER_REGISTRY_PASSWORD" | docker login -u "$DOCKER_REGISTRY_USERNAME" --password-stdin
 build:
 commands:
 - cd day-14/simple-python-app
 - echo "Building Docker Image..."
 - docker build -t $DOCKER_REGISTRY_USERNAME/simple-python-flask-app:latest .
 - docker push $DOCKER_REGISTRY_USERNAME/simple-python-flask-app:latest
 post_build:
 commands:
 - echo "Build Successful"
 - echo "Image pushed to $DOCKER_REGISTRY_USERNAME/simple-python-flask-app:latest"
